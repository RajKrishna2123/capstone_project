# requirements.txt
# python version 3.7
# Core libraries
numpy==1.19.5
opencv-python==4.5.3.56
pandas==1.3.3
h5py-2.10.0
  
# Deep learning framework (TensorFlow GPU version)
tensorflow-gpu==2.4.1
keras==2.4.3

# keras_segmentaion installation form git is recomended 
# Below are the packages needed to be installed separately and are not Python packages
# HDF5 download and install  
# CUDA 11.0 and cuDNN 8.0 specific versions for TensorFlow GPU compatibility
# Be sure to configure your GPU environment accordingly

# OCR (Optical Character Recognition)
paddlepaddle==2.2.0
paddleocr==2.3.0

# Additional libraries (add as needed)
matplotlib==3.4.3
scikit-image==0.18.3
scipy==1.7.3
